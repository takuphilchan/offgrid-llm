# OffGrid LLM - GPU Docker Image (NVIDIA CUDA)
# Build from project root: docker build -f docker/Dockerfile.gpu -t offgrid-llm:gpu .

# =============================================================================
# Stage 1: Build the web application
# =============================================================================
FROM node:22-alpine AS ui-builder

WORKDIR /build/web/app
COPY web/app/package.json web/app/package-lock.json ./
RUN npm ci
COPY web/app/ ./
RUN npm run build

# =============================================================================
# Stage 2: Build the Go binary
# =============================================================================
FROM golang:1.26.6-alpine3.24 AS go-builder

# OffGrid uses the HTTP inference engine; native CUDA/C++ lives in the separate
# llama-builder stage. Match release binaries with a portable, pure-Go build.
WORKDIR /build

# Copy go mod files first for caching
COPY go.mod go.sum ./
RUN go mod download

# Copy source code
COPY cmd/ ./cmd/
COPY internal/ ./internal/
COPY pkg/ ./pkg/

ARG VERSION=dev

# Build the application as a static binary for the CUDA runtime image.
RUN CGO_ENABLED=0 GOOS=linux go build \
    -trimpath -ldflags="-s -w -X main.Version=${VERSION} -extldflags=-static" \
    -o offgrid ./cmd/offgrid/

# =============================================================================
# Stage 3: Build llama.cpp with CUDA support
# =============================================================================
FROM nvidia/cuda:12.8.1-devel-ubuntu24.04 AS llama-builder

ARG LLAMA_CPP_COMMIT=e9fa0781f1c25fc4fe8c86be1edc6970661ad6f0
ARG CUDA_ARCHITECTURES="75;80;86;89;90;120"
ARG BUILD_JOBS=4

RUN apt-get update && apt-get install -y \
    git \
    cmake \
    make \
    g++ \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /build

# Fetch an immutable llama.cpp revision and build it with CUDA
RUN mkdir llama.cpp && cd llama.cpp && \
    git init && \
    git remote add origin https://github.com/ggml-org/llama.cpp.git && \
    git fetch --depth 1 origin ${LLAMA_CPP_COMMIT} && \
    git checkout --detach FETCH_HEAD && \
    cmake -B build \
        -DCMAKE_BUILD_TYPE=Release \
        -DBUILD_SHARED_LIBS=OFF \
        -DGGML_CUDA=ON \
        -DGGML_NATIVE=OFF \
        -DLLAMA_BUILD_SERVER=ON \
        -DLLAMA_BUILD_UI=OFF \
        -DLLAMA_USE_PREBUILT_UI=OFF \
        -DLLAMA_BUILD_TESTS=OFF \
        -DLLAMA_BUILD_EXAMPLES=OFF \
        -DCMAKE_CUDA_ARCHITECTURES="${CUDA_ARCHITECTURES}" && \
    cmake --build build --config Release --parallel "${BUILD_JOBS}" --target llama-server && \
    strip build/bin/llama-server

# =============================================================================
# Stage 4: Final runtime image with CUDA
# =============================================================================
FROM nvidia/cuda:12.8.1-runtime-ubuntu24.04

ARG VERSION=dev
ARG VCS_REF=unknown
ARG BUILD_DATE=unknown

LABEL org.opencontainers.image.title="OffGrid LLM (NVIDIA CUDA)" \
      org.opencontainers.image.description="Private, local-first LLM inference with NVIDIA CUDA acceleration" \
      org.opencontainers.image.url="https://github.com/takuphilchan/offgrid-llm" \
      org.opencontainers.image.source="https://github.com/takuphilchan/offgrid-llm" \
      org.opencontainers.image.licenses="MIT" \
      org.opencontainers.image.version="${VERSION}" \
      org.opencontainers.image.revision="${VCS_REF}" \
      org.opencontainers.image.created="${BUILD_DATE}"

# Install runtime dependencies
RUN apt-get update && apt-get install -y \
    ca-certificates \
    curl \
    wget \
    libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# The Ubuntu-based CUDA image already has UID/GID 1000 (the "ubuntu" account).
# Use that numeric identity so CPU and GPU images can share persistent volumes.

# Create required directories
RUN mkdir -p /var/lib/offgrid/models \
             /var/lib/offgrid/web/ui \
             /var/lib/offgrid/data \
             /var/lib/offgrid/bin && \
    chown -R 1000:1000 /var/lib/offgrid

# Copy binaries from builders
COPY --from=go-builder /build/offgrid /usr/local/bin/offgrid
COPY --from=llama-builder /build/llama.cpp/build/bin/llama-server /var/lib/offgrid/bin/llama-server

RUN chmod +x /usr/local/bin/offgrid /var/lib/offgrid/bin/llama-server

# The NVIDIA runtime injects libcuda at container startup, not during build.
# All other linked libraries must already be present in this final image.
RUN ldd /var/lib/offgrid/bin/llama-server | \
    awk '/not found/ { print; if ($1 != "libcuda.so.1") missing = 1 } END { exit missing }'

# Copy the exact UI built from this source revision.
COPY --from=ui-builder --chown=1000:1000 /build/web/dist /var/lib/offgrid/web/ui

# Switch to non-root user
USER 1000:1000
WORKDIR /var/lib/offgrid

# Expose default port
EXPOSE 11611

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 \
    CMD curl -f http://127.0.0.1:11611/health || exit 1

# Environment variables
ENV OFFGRID_MODELS_DIR=/var/lib/offgrid/models \
    OFFGRID_DATA_DIR=/var/lib/offgrid/data \
    OFFGRID_BIN_DIR=/var/lib/offgrid/bin \
    HOME=/var/lib/offgrid \
    OFFGRID_VERSION=${VERSION} \
    OFFGRID_PORT=11611 \
    OFFGRID_HOST=0.0.0.0 \
    OFFGRID_ALLOW_UNAUTHENTICATED_REMOTE=true \
    OFFGRID_ENABLE_GPU=true \
    OFFGRID_GPU_LAYERS=0

ENTRYPOINT ["/usr/local/bin/offgrid"]
CMD ["serve"]
